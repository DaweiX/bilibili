<UserControl
    x:Class="bilibili.Controls.MyPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:bilibili.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Popup x:Name="pop" VerticalOffset="20" HorizontalAlignment="Right" FlowDirection="RightToLeft">
            <Popup.ChildTransitions>
                <TransitionCollection>
                    <EdgeUIThemeTransition Edge="Left"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <Border Background="{ThemeResource bili_Theme}">
                <TextBlock x:Name="txt_pop" Padding="8" FontSize="16" Foreground="White"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
